# CacheDir contains a path to keep working data and key-value stores.
#
# CacheDir: ~/.cache/gnmatcher

# PgHost is the PostgreSQL host for `gnames` database
#
# PgHost: localhost

# PgUser is a PostgreSQL user with read permissions to `gnames` database.
#
# PgUser: postgres

# PgPass is a PostgreSQL password for `PgUser`.
#
# PgPass: postgres

# PgDB is the name of the PostgreSQL database, usually the name is `gnames`.
#
# PgDB: gnames

# MaxEditDist is the maximum edit distance for fuzzy matching of
# stemmed canonical forms. Can be 1 or 2, 2 is significantly slower.
#
# MaxEditDist: 1

# JobsNum is the number of matching processes running concurrently.
#
# JobsNum: 4

# WebLogsNsqdTCP provides an address to the NSQ messenger TCP service. If
# this value is set and valid, the web logs will be published to the NSQ.
# The option is ignored if `Port` is not set.
#
# If WithWebLogs option is set to `false`, but `WebLogsNsqdTCP` is set to a
# valid URL, the logs will be sent to the NSQ messanging service, but they
# wil not appear as STRERR output.
# Example: `127.0.0.1:4150`
#
# WebLogsNsqdTCP:

# WithWebLogs flag enables logs when running web-service. This flag is
# ignored if `Port` value is not set.
#
# WithWebLogs: false
